{
  "name": "infokelas-root",
  "private": true,
  "workspaces": [
    "packages/frontend",
    "packages/backend"
  ],
  "scripts": {
    "build": "npm run build --workspace packages/frontend && npm run build --workspace packages/backend",
    "start:backend": "node packages/backend/server.js",
    "start:frontend": "npm --workspace packages/frontend start"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^16.6.1",
    "jsonwebtoken": "^9.0.2"
  }
}
